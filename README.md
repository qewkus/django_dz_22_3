# Описание проекта

Учебный проект интернет-магазина с использованием Django фреймворка и Bootstrap.

## Задания к уроку 25

**Задание 1**
-   [x] Переведите имеющиеся в проекте контроллеры с FBV на CBV.
-   [x] Не забудьте про контроллер страницы контактов. Для его замены можно воспользоваться `View` или `TemplateView`.

**Задание 2**
-   [x] Создайте новое приложение для блога и добавьте его в файл `settings.py`.
-   [x] Создайте новую модель блоговой записи со следующими полями:
-   [x] заголовок,
-   [x] содержимое,
-   [x] превью (изображение),
-   [x] дата создания,
-   [x] признак публикации (булевое поле),
-   [x] количество просмотров.
-   [x] Для работы с блогом реализуйте полный CRUD для новой модели, используя CBV.

**Задание 3**
-   [x] Модифицируйте вывод и обработку запросов, добавив следующую логику на уровне контроллеров:
-   [x] Увеличение счетчика просмотров: при открытии отдельной статьи увеличивать счетчик просмотров.
> **Подсказка**
> В этом задании необходимо переопределить метод `get_object` в `DetailView`.
-   [x] Фильтрация опубликованных статей: выводить в список статей только те, которые имеют положительный признак публикации.
> **Подсказка**
> В этом задании необходимо переопределить метод `get_queryset` в `ListView`.
-   [x] Перенаправление после редактирования: после успешного редактирования записи необходимо перенаправлять пользователя на просмотр этой статьи.
> **Подсказка**
> В этом задании необходимо использовать параметр `success_url` в `UpdateView`.

-   **Дополнительное задание***
    -   [ ] Когда статья достигает 100 просмотров, отправляйте себе на почту поздравление с достижением.

> **Примечание:** Дополнительное задание, помеченное звездочкой, желательно, но не обязательно выполнять.

## Основные требования

**Работа с Git и GitHub**
-   [x] Домашка сдана через pull request из ветки домашней работы в ветку `develop`.
-   [x] В коммиты не добавлены игнорируемые файлы.

- **Базовые настройки проекта**
-   [ ] В проекте есть файл с зависимостями.

- **Перевод шаблонов с FBV на CBV**
-   [ ] Все контроллеры приложения `catalog` переведены в CBV.

- **Приложение блога**
-   [ ] Создано приложение блога.
-   [ ] Приложение блога зарегистрировано в `INSTALLED_APPS`.
-   [ ] Создан файл `urls.py` в приложении блога.

**Модель блоговой записи**
-   [ ] Создана модель блоговой записи.
-   [ ] Модель блоговой записи содержит необходимые поля.
-   [ ] В модели блоговой записи описан класс `Meta`, указаны параметры `verbose_name` и `verbose_name_plural`.
-   [ ] В модели блоговой записи реализован метод `__str__`.

- **Работа с блоговыми записями**
-   [ ] Для блоговой записи реализован полный CRUD.
-   [ ] CRUD для блоговой записи создан с помощью CBV.
-   [ ] Шаблоны блоговой записи используют базовый шаблон.
-   [ ] Шаблоны блоговой записи используют подшаблон с главным меню.

**Маршрутизация**
-   [ ] Контроллеры зарегистрированы на адреса `blogs/...`.
-   [ ] В основном URL-файле использован `include` для подключения URL-файла приложения блога.
-   [ ] Все URL-адреса в проекте заканчиваются на `/`.

**Модифицирование работы с блоговыми записями**
-   [ ] При открытии отдельной статьи увеличивается счетчик просмотров статьи.
-   [ ] На страницу списка статей выводятся только статьи, которые имеют положительный признак публикации.
-   [ ] После успешного редактирования записи происходит перенаправление пользователя на страницу отредактированной статьи.